{ config, pkgs, ... }:

{
  # Add any system-level overrides here
  # This would typically include things that would otherwise go in configuration.nix
  
  # For example:
  # environment.systemPackages = with pkgs; [
  #   firefox
  # ];
}
